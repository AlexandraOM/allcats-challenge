import styled from 'styled-components';

const P = styled.p`
  text-align: start;
  margin: 0;
  padding: var(--space-xs);
`;
const H1 = styled.h1`
  text-align: start;
  margin: 0;
  padding: var(--space-xs);
  width: 100%;
`;

export { P, H1 };
