import styled from 'styled-components';

const P = styled.p`
  text-align: start;
  margin: 0;
  padding: var(--space-xs);
  font-weight: 300;
`;
const H1 = styled.h1`
  text-align: start;
  margin: 0;
  padding: var(--space-s);
  width: 100%;
`;

export { P, H1 };
